.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
SECRET_CONFIG
.idea
!.env.ci
*.env
gcp-*.json
dot_env_local.json

chat-ui_modifications/

NOTES.md
CLAUDE.md

# Local-only scratch space for manual/Docker testing (configs, throwaway
# scripts, notes) - never committed, whole folder ignored regardless of
# what gets added under it.
/local-testing/
